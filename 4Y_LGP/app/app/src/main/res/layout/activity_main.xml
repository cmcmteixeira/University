<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity"
    android:gravity="center_vertical|center_horizontal"
    android:id="@+id/Layout"
    android:background="#ff009788">

    <ProgressBar
        style="@style/Base.Widget.AppCompat.ProgressBar.Horizontal"
        android:layout_width="281dp"
        android:layout_height="wrap_content"
        android:id="@+id/progressExtraction"
        android:height="200dp"
        android:indeterminate="false"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceLarge"
        android:text="Verifying Files..."
        android:id="@+id/textViewInfo"
        android:layout_above="@+id/progressExtraction"
        android:layout_alignLeft="@+id/progressExtraction"
        android:layout_alignStart="@+id/progressExtraction"
        android:layout_marginBottom="45dp"
        android:layout_alignRight="@+id/progressExtraction"
        android:layout_alignEnd="@+id/progressExtraction"
        android:gravity="center_horizontal"
        android:textSize="20dp"
        android:autoText="false"
        android:textColor="#ffffffff" />

</RelativeLayout>
