<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    android:id="@+id/user_container_layout"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical"
    xmlns:android="http://schemas.android.com/apk/res/android">


    <ListView
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:id="@+id/user_ticketList_listView"
        android:layout_above="@+id/user_layout_bottom_button"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true">

    </ListView>

    <LinearLayout
        android:id="@+id/user_layout_bottom_button"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true" >
        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/user_btn_purchase"
            android:id="@+id/user_btn_purchase"/>

    </LinearLayout>

</RelativeLayout>